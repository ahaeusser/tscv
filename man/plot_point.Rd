% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_point.R
\name{plot_point}
\alias{plot_point}
\title{Plot data as a scatterplot}
\usage{
plot_point(
  data,
  x,
  y,
  facet_var = NULL,
  facet_scale = "free",
  facet_nrow = NULL,
  facet_ncol = NULL,
  color = NULL,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  caption = NULL,
  point_size = 1.5,
  point_type = 16,
  point_color = "grey35",
  point_alpha = 1,
  theme_set = theme_tscv(),
  theme_config = list(),
  ...
)
}
\arguments{
\item{data}{A \code{data.frame}, \code{tibble}, or \code{tsibble} in long
format.}

\item{x}{Unquoted column in \code{data} used on the x-axis.}

\item{y}{Unquoted column in \code{data} containing numeric values shown on
the y-axis.}

\item{facet_var}{Optional unquoted column in \code{data} used for faceting.}

\item{facet_scale}{Character value defining facet axis scaling. Common values
are \code{"free"}, \code{"fixed"}, \code{"free_x"}, and \code{"free_y"}.}

\item{facet_nrow}{Optional integer. Number of rows in the facet layout.}

\item{facet_ncol}{Optional integer. Number of columns in the facet layout.}

\item{color}{Optional unquoted column in \code{data} used to map point color.}

\item{title}{Character value. Plot title.}

\item{subtitle}{Character value. Plot subtitle.}

\item{xlab}{Character value. Label for the x-axis.}

\item{ylab}{Character value. Label for the y-axis.}

\item{caption}{Character value. Plot caption.}

\item{point_size}{Numeric value defining the point size.}

\item{point_type}{Numeric or character value defining the point shape.}

\item{point_color}{Character value defining the point color. Ignored when
\code{color} is supplied.}

\item{point_alpha}{Numeric value between \code{0} and \code{1} defining point
transparency.}

\item{theme_set}{A complete \code{ggplot2} theme.}

\item{theme_config}{A named \code{list} with additional arguments passed to
\code{ggplot2::theme()}.}

\item{...}{Currently not used.}
}
\value{
An object of class \code{ggplot}.
}
\description{
Create a scatterplot for two variables.
}
\details{
\code{plot_point()} is a convenience wrapper around
\code{ggplot2::geom_point()}. It is useful for plotting relationships between
two variables, for example observed values over time, forecast errors by
horizon, or one numeric diagnostic against another.

The arguments \code{x}, \code{y}, \code{facet_var}, and \code{color} are
passed as unquoted column names.

If \code{color} is supplied, point colors are mapped to that variable and
\code{point_color} is ignored. If \code{color} is not supplied, all points are
drawn using \code{point_color}.

Additional theme settings can be supplied through \code{theme_config}. This
should be a named list of arguments passed to \code{ggplot2::theme()}.
}
\examples{
library(dplyr)

data <- M4_monthly_data |>
  filter(series == "M23100")

plot_point(
  data = data,
  x = index,
  y = value,
  title = "M4 Monthly Time Series",
  subtitle = "Series M23100",
  xlab = "Time",
  ylab = "Value"
)

acf_data <- estimate_acf(
  .data = M4_monthly_data |>
    filter(series \%in\% c("M23100", "M14395")),
  context = list(
    series_id = "series",
    value_id = "value",
    index_id = "index"
  ),
  lag_max = 12
)

plot_point(
  data = acf_data,
  x = lag,
  y = value,
  color = series,
  title = "Autocorrelation by Series",
  subtitle = "Sample autocorrelation up to lag 12",
  xlab = "Lag",
  ylab = "ACF"
)
}
\seealso{
Other data visualization: 
\code{\link{plot_bar}()},
\code{\link{plot_density}()},
\code{\link{plot_histogram}()},
\code{\link{plot_line}()},
\code{\link{plot_qq}()}
}
\concept{data visualization}
