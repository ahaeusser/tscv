% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_data.R
\name{estimate_acf}
\alias{estimate_acf}
\title{Estimate autocorrelations by time series}
\usage{
estimate_acf(.data, context, lag_max = 24, level = 0.9, ...)
}
\arguments{
\item{.data}{A \code{tibble} containing the time series data.}

\item{context}{A named \code{list} with the identifiers for
\code{series_id}, \code{value_id}, and \code{index_id}.}

\item{lag_max}{Integer. Maximum lag for which the autocorrelation is
estimated.}

\item{level}{Numeric value. Confidence level used to calculate the
approximate significance bound.}

\item{...}{Further arguments passed to \code{stats::acf()}.}
}
\value{
A \code{tibble} with the series identifier and the columns \code{type},
\code{lag}, \code{value}, \code{bound}, and \code{sign}.
}
\description{
Estimate the sample autocorrelation function for one or more time series in a
\code{tibble}.
}
\details{
\code{estimate_acf()} groups the input data by the series identifier supplied
in \code{context} and estimates the sample autocorrelation function for each
time series separately.

The output contains one row per series and lag. The column \code{bound}
contains an approximate significance threshold based on the selected
confidence level. The logical column \code{sign} indicates whether the
absolute autocorrelation is larger than this threshold.
}
\examples{
library(dplyr)

context <- list(
  series_id = "series",
  value_id = "value",
  index_id = "index"
)

data <- M4_monthly_data |>
  filter(series \%in\% c("M23100", "M14395"))

estimate_acf(
  .data = data,
  context = context,
  lag_max = 12
)
}
\seealso{
Other data analysis: 
\code{\link{acf_vec}()},
\code{\link{estimate_kurtosis}()},
\code{\link{estimate_mode}()},
\code{\link{estimate_pacf}()},
\code{\link{estimate_skewness}()},
\code{\link{pacf_vec}()},
\code{\link{summarise_data}()},
\code{\link{summarise_split}()},
\code{\link{summarise_stats}()}
}
\concept{data analysis}
