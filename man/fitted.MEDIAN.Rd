% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-fable.R
\name{fitted.MEDIAN}
\alias{fitted.MEDIAN}
\title{Extract fitted values from a median model}
\usage{
\method{fitted}{MEDIAN}(object, ...)
}
\arguments{
\item{object}{A fitted \code{MEDIAN} model object.}

\item{...}{Additional arguments. Currently not used.}
}
\value{
Fitted values.
}
\description{
Extract fitted values from a fitted \code{MEDIAN} model.
}
\examples{
library(dplyr)
library(tsibble)
library(fabletools)

train_frame <- M4_monthly_data |>
  filter(series == first(series)) |>
  as_tsibble(index = index)

model_frame <- train_frame |>
  model("MEDIAN" = MEDIAN(value ~ window()))

fitted(model_frame)
}
\seealso{
Other MEDIAN: 
\code{\link{MEDIAN}()},
\code{\link{forecast.MEDIAN}()},
\code{\link{model_sum.MEDIAN}()},
\code{\link{residuals.MEDIAN}()}
}
\concept{MEDIAN}
