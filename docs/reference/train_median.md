# Median model

Train median model (MEDIAN).

## Usage

``` r
train_median(.data, specials, ...)
```

## Arguments

- .data:

  Input data as tsibble.

- specials:

  Specials as list defined in `specials_median`.

- ...:

  Currently not in use.

## Value

An object of class `MEDIAN`.
