# Create platform info

Create a `glue` object with the platform of the current R session.

## Usage

``` r
log_platform(...)
```

## Arguments

- ...:

  Arguments passed to
  [`glue_header()`](https://ahaeusser.github.io/tscv/reference/glue_header.md).

## Value

An object of class `glue`.
