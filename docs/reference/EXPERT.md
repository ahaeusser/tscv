# Automatic train a EXPERT model

Automatic train the EXPERT model.

## Usage

``` r
EXPERT(formula, ...)
```

## Arguments

- formula:

  Model specification (see "Specials" section, currently not in use ...)

- ...:

  Further arguments (currently not in use).

## Value

expert_model An object of class `EXPERT`.
