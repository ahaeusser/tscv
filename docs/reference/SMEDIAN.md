# Seasonal median model

Automatic train a seasonal median model (SMEDIAN).

## Usage

``` r
SMEDIAN(formula, ...)
```

## Arguments

- formula:

  Model specification (see "Specials" section, currently not in use
  ...).

- ...:

  Further arguments.

## Value

smedian_model An object of class `SMEDIAN`.
