# Helper function to create numbered strings.

Creates a character vector in the form `c("x(1)", x(2), ..., x(n))`.

## Usage

``` r
number_string(x, n)
```

## Arguments

- x:

  Character value.

- n:

  Integer value.

## Value

x Character vector.
