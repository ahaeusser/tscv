# Median model

Automatic train a median model (MEDIAN).

## Usage

``` r
MEDIAN(formula, ...)
```

## Arguments

- formula:

  Model specification (see "Specials" section, currently not in use
  ...).

- ...:

  Further arguments.

## Value

median_model An object of class `MEDIAN`.
